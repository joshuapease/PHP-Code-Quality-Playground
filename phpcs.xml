<?xml version="1.0"?>
<ruleset name="PHP_CodeSniffer">
    <description>
        Custom PHP_CodeSniffer Configuration
    </description>

    <file>./web</file>

    <rule ref="Squiz.PHP.DisallowInlineIf"/>

    <!-- Not sure if this is working. What is even in PSR12 anyway? -->
    <rule ref="PSR12">
        <type>error</type>
    </rule>
</ruleset>